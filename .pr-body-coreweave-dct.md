## Source

- Synced from personal monorepo **`Atllas_Coreweave`** branch **`main`** (includes merged PR #1 *Optic_Count: clarify Count vs Postgres optic totals*).
- **PR branch:** `pr/lamars-atlas-main-sync` — branched from **`main`** here, then the tree was replaced with **`git subtree split -P DCT_Scripts`** from Atlas (`6282c4a` tip) so GitHub can open a normal PR. **`lamars-branch`** still holds the subtree-only history (no merge base with `main`; `gh pr create --base main --head lamars-branch` fails with *no history in common*).

## Summary of changes

### NetBox (GraphQL + dashboard)

- **`Optic_Count/Source_count_Netbox.py`**: `User-Agent` on GraphQL requests; **`_graphql_paginated`** using NetBox **`pagination: { start, limit }`** (1000-row cap, id-based cursor per internal guidance); paginated **`device_list`** / **`interface_list`** in **`_query_single_site`**; retries for transient errors unchanged.
- **`Optic_Count/netbox_dashboard_ingest.py`**: Dashboard snapshot ingest uses the same paginated GraphQL pattern for per–data-hall and site-wide passes; site-wide dedup rows keep **real `location_slug`** (avoids empty-DH mis-bucketing); docstrings aligned with pagination; unused imports removed.
- **`Optic_Count/netbox_dashboard_routes.py`**: Dashboard SQL excludes rows with empty **`name`** / **`device_name`**; **`COUNT(DISTINCT name)`** where row inflation was possible; **`location_count`** ignores blank **`location_slug`**; rack and optics queries aligned.

### Optic_Count / Atlas (broader PR scope)

- **`sql_templates.py`** extraction and **`atlas_query_router.py`** refactor; **`atlas_data_loader.py`** connection rollback + JSONB ingest fix; cutsheet pipeline, **`query_intent` / `query_extractors`**, tests, **`atlas_schema.sql`**, Helm, Docker, **`requirements*.txt`**, **`atlas_web_app`** / NetBox blueprint wiring, docs (`AGENTS.md`, `netbox_issues.md`, etc.).

### Commits on this export (newest first)

```
6282c4a fix(netbox-dashboard): consistent counts and non-empty device names
a49ee49 fix(netbox): paginated ingest docs, location_slug on site-wide pass, trim imports
824d0e0 Enhance AGENTS.md with detailed coding instructions…
14c7aad Enhance Source_count_Netbox.py with GraphQL request retries and pagination…
743ce98 Optic_Count: label sheet cell counts vs deduped DB optic totals
c5300d8 Add per-developer ignore for orchestrator/IDE state
8b7f6cc Wave 1-3 audit fixes + critical jsonb cast regression
f6b3265 Initial commit: Atllas_Coreweave codebase
```

## How to review

- The **Files** tab is a full diff vs `main` (includes removal of junk that was tracked on `main` under `.claude/`, `.omc/`, `.omx/`, `.playwright-mcp/`, etc., if present). Prefer the **commit list** above for intent; the single merge commit on this PR squashes the logical history.
- **Run / test** (from `DCT_Scripts/Optic_Count`): see **`AGENTS.md`** for Docker vs venv, pytest, and NetBox env (`NETBOX_API_TOKEN`).

## Risk / follow-ups

- **`lamars-branch`** was updated with **`--force-with-lease`** against the previously known tip **`010817c0`**; if others had pushed since fetch, the push would have failed (it succeeded).
- Consider a follow-up PR to **`main`** only after spot-checking critical paths (ingest, router, auth) on a CoreWeave checkout.
