# Optional extras for local development only — not installed in the Docker image.
# After runtime deps: pip install -r requirements-dev.txt
#
-r requirements.txt

pytest>=8.0,<9.0
graphifyy==0.4.23

# Required to build cutsheet_normalizer_fast.pyx locally. Production
# builds install Cython inside the Docker builder stage.
Cython>=3.0,<4.0
