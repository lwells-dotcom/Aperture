
# Orchestrator and IDE state (per-developer, never commit)
.omc/
.omx/
.codex/
.playwright-mcp/

# Uploaded cutsheet files (runtime data, not source)
uploads/

# Cython build artifacts (regenerated by setup.py / Dockerfile)
*.c
*.so
build/

# Per-session scratch notes (developer-local, never commit)
session-notes-*.md

